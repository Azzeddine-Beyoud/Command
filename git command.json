git clone
git status 
git add      to add the file in steagin eara

git commit -m "hello"
git reset head osama.txt         to delete the from steage area

git remote -v
git push origin main 
git pull origin main      this command (pull) do two thing in same time. 1 is git fetch(get the new t3dilat in the remote repo), 2 is git merge is do merging between remote and local


config :

git config -l
git config -l --show-origin
git config --global user.email
git config --global user.email "azzo07@hotmail.fr"
git config --global user.email ""
git config --global --unset user.email 
git config --global --edit  (GUI)

Erreur dans la saisie 

pub key:

ssh-keygen -t rsa -b 4096 -C "azzo07@hotmail.fr"
~/.ssh/id_rsa.pub
ssh -T git@github.com


Project already created in the past and i want to uplod it to github :

 1 git init 
 2 go to github and create a new repo
 3 git remote add origin https://github.com/Azzeddine-Beyoud/tutorial.git
 4 git push -u origin main   (-u for doing pull first and after that doing push)


git status 
git add      to add the file in steagin eara

git commit -m "hello"
git reset head osama.txt         to delete the from steage area

git remote -v
git push origin main 
git pull origin main      this command (pull) do two thing in same time. 1 is git fetch(get the new t3dilat in the remote repo), 2 is git merge is do merging between remote and local


 #05 - Push Local Changes To Remote Repository
    git push origin main 

 #06 - Pull Changes From Remote Repository
    git pull origin main        this command do two somthing on same tima first git fetch(get the new modification on the remote repo) 
                             & second git merge (merge the local with the remote) 

 #07 - Everything About Git Configurations
 
    git config -l
    git config -list
    git help config 
    git config -l --show-origin
    git config --global user.email
    git config --global user.email "azzo07@hotmail.fr"
    git config --global user.email ""
    git config --global --unset user.email  to delete the user name 
    git config --global --edit  (GUI)


#08 - Generate And Test Github Public Key:
pub key:

ssh-keygen -t rsa -b 4096 -C "azzo07@hotmail.fr"
~/.ssh/id_rsa.pub
ssh -T git@github.com


#09 - Create Repository From Existing Project


Project already created in the past and i want to uplod it to github :

 1 git init 
 2 touch index.html
 3 git add index.html
 4 git commit -m "first commit"
 5 go to github and create a new repo
 6 git remote add origin https://github.com/Azzeddine-Beyoud/tutorial.git
 7 git push -u origin main   (-u for doing pull first and after that doing push)


 #10 - Learn Pull Request With Real Examples
      0 command line

 #11 - Learn Everything About Aliases
 git config --global alias.st status     st = status
 git config --global alias.st 
 git st 
 git config --global alias.cm "commit -m"
 git config --global --edit 
 git alias in google for get more foumos alias


 #12 - Learn About Branching And Merging

 git branch 
 git branch scroll         to create new branch 
 git checkout scroll       to swith to this branch 
 git branch -d scroll      (-d do some check before deleting if it has some modification so it is not deleted)
 git branch -D scroll      (-D force deleted)
 git checkout -b develepment     create the branch and switch to them 
 git branch -m deve              to change the name of branch 
 touch index.html 
 git add index.html 
 git commit -m "jdkfdjf"

 merge master with deve:
 git checkout master 
 git merge deve
 git branch -d deve
 git push origin master

 send pull request from branch to the origin directly 
i'm in branch 

git push origin scroll 
go to the site to accepte the pull request 



